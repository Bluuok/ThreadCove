{
  "name": "@threadcove/webui",
  "version": "0.1.0",
  "description": "WebUI: reuses the same transport + CHANNEL_MAP, overrides only LOCAL_ONLY methods",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "vite",
    "build": "vite build",
    "test": "bun test"
  },
  "dependencies": {
    "@threadcove/core": "workspace:*",
    "@threadcove/shared": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@threadcove/ui": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
